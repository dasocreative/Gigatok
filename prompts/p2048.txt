<bos>Memory bandwidth is the binding constraint for single-stream autoregressive decoding on unified-memory systems. Each generated token requires reading the full set of active weights from DRAM, plus the key and value tensors for every attending layer. Arithmetic intensity is therefore near one operation per byte, and the achievable token rate is bounded above by effective bandwidth divided by bytes touched per token. Kernel fusion reduces intermediate traffic; quantization reduces weight traffic; windowed attention bounds cache traffic; speculative decoding amortizes weight traffic across several accepted tokens at once. Memory bandwidth is the binding constraint for single-stream autoregressive decoding on unified-memory systems. Each generated token requires reading the full set of active weights from DRAM, plus the key and value tensors for every attending layer. Arithmetic intensity is therefore near one operation per byte, and the achievable token rate is bounded above by effective bandwidth divided by bytes touched per token. Kernel fusion reduces intermediate traffic; quantization reduces weight traffic; windowed attention bounds cache traffic; speculative decoding amortizes weight traffic across several accepted tokens at once. Memory bandwidth is the binding constraint for single-stream autoregressive decoding on unified-memory systems. Each generated token requires reading the full set of active weights from DRAM, plus the key and value tensors for every attending layer. Arithmetic intensity is therefore near one operation per byte, and the achievable token rate is bounded above by effective bandwidth divided by bytes touched per token. Kernel fusion reduces intermediate traffic; quantization reduces weight traffic; windowed attention bounds cache traffic; speculative decoding amortizes weight traffic across several accepted tokens at once. Memory bandwidth is the binding constraint for single-stream autoregressive decoding on unified-memory systems. Each generated token requires reading the full set of active weights from DRAM, plus the key and value tensors for every attending layer. Arithmetic intensity is therefore near one operation per byte, and the achievable token rate is bounded above by effective bandwidth divided by bytes touched per token. Kernel fusion reduces intermediate traffic; quantization reduces weight traffic; windowed attention bounds cache traffic; speculative decoding amortizes weight traffic across several accepted tokens at once. Memory bandwidth is the binding constraint for single-stream autoregressive decoding on unified-memory systems. Each generated token requires reading the full set of active weights from DRAM, plus the key and value tensors for every attending layer. Arithmetic intensity is therefore near one operation per byte, and the achievable token rate is bounded above by effective bandwidth divided by bytes touched per token. Kernel fusion reduces intermediate traffic; quantization reduces weight traffic; windowed attention bounds cache traffic; speculative decoding amortizes weight traffic across several accepted tokens at once. Memory bandwidth is the binding constraint for single-stream autoregressive decoding on unified-memory systems. Each generated token requires reading the full set of active weights from DRAM, plus the key and value tensors for every attending layer. Arithmetic intensity is therefore near one operation per byte, and the achievable token rate is bounded above by effective bandwidth divided by bytes touched per token. Kernel fusion reduces intermediate traffic; quantization reduces weight traffic; windowed attention bounds cache traffic; speculative decoding amortizes weight traffic across several accepted tokens at once. Memory bandwidth is the binding constraint for single-stream autoregressive decoding on unified-memory systems. Each generated token requires reading the full set of active weights from DRAM, plus the key and value tensors for every attending layer. Arithmetic intensity is therefore near one operation per byte, and the achievable token rate is bounded above by effective bandwidth divided by bytes touched per token. Kernel fusion reduces intermediate traffic; quantization reduces weight traffic; windowed attention bounds cache traffic; speculative decoding amortizes weight traffic across several accepted tokens at once. Memory bandwidth is the binding constraint for single-stream autoregressive decoding on unified-memory systems. Each generated token requires reading the full set of active weights from DRAM, plus the key and value tensors for every attending layer. Arithmetic intensity is therefore near one operation per byte, and the achievable token rate is bounded above by effective bandwidth divided by bytes touched per token. Kernel fusion reduces intermediate traffic; quantization reduces weight traffic; windowed attention bounds cache traffic; speculative decoding amortizes weight traffic across several accepted tokens at once. Memory bandwidth is the binding constraint for single-stream autoregressive decoding on unified-memory systems. Each generated token requires reading the full set of active weights from DRAM, plus the key and value tensors for every attending layer. Arithmetic intensity is therefore near one operation per byte, and the achievable token rate is bounded above by effective bandwidth divided by bytes touched per token. Kernel fusion reduces intermediate traffic; quantization reduces weight traffic; windowed attention bounds cache traffic; speculative decoding amortizes weight traffic across several accepted tokens at once. Memory bandwidth is the binding constraint for single-stream autoregressive decoding on unified-memory systems. Each generated token requires reading the full set of active weights from DRAM, plus the key and value tensors for every attending layer. Arithmetic intensity is therefore near one operation per byte, and the achievable token rate is bounded above by effective bandwidth divided by bytes touched per token. Kernel fusion reduces intermediate traffic; quantization reduces weight traffic; windowed attention bounds cache traffic; speculative decoding amortizes weight traffic across several accepted tokens at once. Memory bandwidth is the binding constraint for single-stream autoregressive decoding on unified-memory systems. Each generated token requires reading the full set of active weights from DRAM, plus the key and value tensors for every attending layer. Arithmetic intensity is therefore near one operation per byte, and the achievable token rate is bounded above by effective bandwidth divided by bytes touched per token. Kernel fusion reduces intermediate traffic; quantization reduces weight traffic; windowed attention bounds cache traffic; speculative decoding amortizes weight traffic across several accepted tokens at once. Memory bandwidth is the binding constraint for single-stream autoregressive decoding on unified-memory systems. Each generated token requires reading the full set of active weights from DRAM, plus the key and value tensors for every attending layer. Arithmetic intensity is therefore near one operation per byte, and the achievable token rate is bounded above by effective bandwidth divided by bytes touched per token. Kernel fusion reduces intermediate traffic; quantization reduces weight traffic; windowed attention bounds cache traffic; speculative decoding amortizes weight traffic across several accepted tokens at once. Memory bandwidth is the binding constraint for single-stream autoregressive decoding on unified-memory systems. Each generated token requires reading the full set of active weights from DRAM, plus the key and value tensors for every attending layer. Arithmetic intensity is therefore near one operation per byte, and the achievable token rate is bounded above by effective bandwidth divided by bytes touched per token. Kernel fusion reduces intermediate traffic; quantization reduces weight traffic; windowed attention bounds cache traffic; speculative decoding amortizes weight traffic across several accepted tokens at once. Memory bandwidth is the binding constraint for single-stream autoregressive decoding on unified-memory systems. Each generated token requires reading the full set of active weights from DRAM, plus the key and value tensors for every attending layer. Arithmetic intensity is therefore near one operation per byte, and the achievable token rate is bounded above by effective bandwidth divided by bytes touched per token. Kernel fusion reduces intermediate traffic; quantization reduces weight traffic; windowed attention bounds cache traffic; speculative decoding amortizes weight traffic across several accepted tokens at once. Memory bandwidth is the binding constraint for single-stream autoregressive decoding on unified-memory systems. Each generated token requires reading the full set of active weights from DRAM, plus the key and value tensors for every attending layer. Arithmetic intensity is therefore near one operation per byte, and the achievable token rate is bounded above by effective bandwidth divided by bytes touched per token. Kernel fusion reduces intermediate traffic; quantization reduces weight traffic; windowed attention bounds cache traffic; speculative decoding amortizes weight traffic across several accepted tokens at once. Memory bandwidth is the binding constraint for single-stream autoregressive decoding on unified-memory systems. Each generated token requires reading the full set of active weights from DRAM, plus the key and value tensors for every attending layer. Arithmetic intensity is therefore near one operation per byte, and the achievable token rate is bounded above by effective bandwidth divided by bytes touched per token. Kernel fusion reduces intermediate traffic; quantization reduces weight traffic; windowed attention bounds cache traffic; speculative decoding amortizes weight traffic across several accepted tokens at once. Memory bandwidth is the binding constraint for single-stream autoregressive decoding on unified-memory systems. Each generated token requires reading the full set of active weights from DRAM, plus the key and value tensors for every attending layer. Arithmetic intensity is therefore near one operation per byte, and the achievable token rate is bounded above by effective bandwidth divided by bytes touched per token. Kernel fusion reduces intermediate traffic; quantization reduces weight traffic; windowed attention bounds cache traffic; speculative decoding amortizes weight traffic across several accepted tokens at once. Memory bandwidth is the binding constraint for single-stream autoregressive decoding on unified-memory systems. Each generated token requires reading the full set of active weights from DRAM, plus the key and value tensors for every attending layer. Arithmetic intensity is therefore near one operation per byte, and the achievable token rate is bounded above by effective bandwidth divided by bytes touched per token. Kernel fusion reduces intermediate traffic; quantization reduces weight traffic; windowed attention bounds cache traffic; speculative decoding amortizes weight traffic across several accepted tokens at once. Memory bandwidth is the binding constraint for single-stream autoregressive decoding on unified-memory systems. Each generated token requires reading the full set of active weights from DRAM, plus the key and value tensors for every attending layer. Arithmetic intensity is therefore near one operation per byte, and the achievable token rate is bounded above by effective bandwidth divided by bytes touched per token. Kernel fusion reduces intermediate traffic; quantization reduces weight traffic; windowed attention bounds cache traffic; speculative decoding amortizes weight traffic across several accepted tokens at once. Memory bandwidth is the binding constraint for single-stream autoregressive decoding on unified-memory systems. Each generated token requires reading the full set of active weights from DRAM, plus the key and value tensors for every attending layer. Arithmetic intensity is therefore near one operation per byte, and the achievable token rate is bounded above by effective bandwidth divided by bytes touched per token